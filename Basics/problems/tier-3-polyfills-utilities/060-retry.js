// Retry an asynchronous operation after failures.
// Input: retry(request, 3, 100)
// Output: request is attempted up to 3 times before rejecting
function retry(operation, attempts, delay) {
    // TODO: implement
}
