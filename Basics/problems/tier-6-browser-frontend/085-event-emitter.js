// Implement an event emitter with on, off, and emit.
// Input: emitter.on('ready', handler); emitter.emit('ready')
// Output: handler runs when the 'ready' event is emitted.
function createEventEmitter() {
    // TODO: implement
}
