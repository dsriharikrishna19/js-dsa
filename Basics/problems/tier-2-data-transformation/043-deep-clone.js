// Deep clone an object without shared nested references.
// Input: { user: { name: 'Ana' }, tags: ['js'] }
// Output: An equal object whose nested values are independent copies.
function deepClone(value) {
    // TODO: implement
}
