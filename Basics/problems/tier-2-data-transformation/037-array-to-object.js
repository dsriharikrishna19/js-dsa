// Convert an array of objects to an object keyed by a property.
// Input: [{ id: 'a', value: 10 }, { id: 'b', value: 20 }], 'id'
// Output: { a: { id: 'a', value: 10 }, b: { id: 'b', value: 20 } }
function arrayToObject(items, key) {
    // TODO: implement
}
