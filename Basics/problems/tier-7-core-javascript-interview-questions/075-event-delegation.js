// Simulate event delegation in a simple parent-child DOM example.
// Goal: handle clicks on child elements using a single parent listener.
function setupEventDelegation(parentId) {
    // TODO: implement event delegation logic
}

// Example usage:
// const parent = document.getElementById('parent');
// setupEventDelegation('parent');
// Clicking any child button should trigger the handler with the clicked target.
