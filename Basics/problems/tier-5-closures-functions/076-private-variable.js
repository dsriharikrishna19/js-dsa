// Create a private variable using a closure.
// Input: createPrivateValue(10), then setValue(20)
// Output: getValue() returns 20; the value is not directly accessible.
function createPrivateValue(initialValue) {
    // TODO: implement
}
