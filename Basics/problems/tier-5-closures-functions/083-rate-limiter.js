// Implement a simple function rate limiter.
// Input: rateLimit(save, 1000), called 3 times within 1 second
// Output: save runs according to the configured rate.
function rateLimit(callback, interval) {
    // TODO: implement
}
