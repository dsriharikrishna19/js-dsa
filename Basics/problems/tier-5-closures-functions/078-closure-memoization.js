// Implement memoization using a closure.
// Input: memoized(5) called twice
// Output: Same result twice; the original callback runs once.
function createMemoizedFunction(callback) {
    // TODO: implement
}
